{
  "name": "animeclient",
  "version": "1.0.5",
  "main": "index.js",
  "description": "Une application qui regroupe plusieurs platformes de streaming d'animes vostfr/vf",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . AnimeClient --overwrite --win32 --icon=./build/logo.png --out=dist --prune=false --asar",
    "win": "electron-packager . AnimeClient --overwrite --platform=win32 --arch=ia32 --icon=./build/logo.png --out=dist --prune=false --asar",
    "darwin": "electron-packager . AnimeClient --overwrite --platform=darwin --arch=x64 --icon=./build/logo.icns --out=dist --prune=false --asar",
    "linux": "electron-packager . AnimeClient --overwrite --platform=linux --arch=x64 --icon=./build/logo.png --out=dist --prune=false --asar",
    "postinstall": "node-prune",
    "clean": "modclean"
  },
  "author": "zvbt",
  "license": "MIT",
  "dependencies": {
    "@cliqz/adblocker-electron": "^1.23.6",
    "cross-fetch": "^3.1.5",
    "discord-rich-presence": "^0.0.8",
    "fetch": "^1.1.0",
    "fs": "^0.0.1-security",
    "infinite-loop": "^0.2.2",
    "ts-node": "^10.5.0"
  },
  "devDependencies": {
    "electron": "^17.1.1",
    "electron-packager": "^15.4.0"
  }
}
